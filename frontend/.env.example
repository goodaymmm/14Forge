# n8n Webhook Configuration
# If n8n is running in Docker on default settings:
VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/14coacher

# If n8n is running on a different port or host:
# VITE_N8N_WEBHOOK_URL=http://your-n8n-host:port/webhook/14coacher

# Note: The webhook path must match what you configured in n8n workflow